 /* developed by Nahushkumar Nitinbhai Rana
    Including A* algorthim with 2 heristics and 2 Types of display Mode.*/
    
Requirement = Must have this both header file in include directory.
	#include <cstdlib>
	#include <bits/stdc++.h>

To run the program you can directly open astar application in project folder or can compile and execute the file.


To Compile and Run the program -

1. Open CMD prompt
2. Change CMD directory to project file, 
	cd C:\Users\tiger\Desktop\Astar Program
3. Compile the program using this line. 
	g++ -o astar newClass.cpp Node.cpp
4. Run the Program. 
	astar